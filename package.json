{
  "name": "plexi-beer-mobile",
  "version": "1.0.0",
  "private": true,
  "description": "Coque iOS Plexi Beer — Capacitor + AltStore (phase 1 perso)",
  "scripts": {
    "postinstall": "node scripts/patch-ios-plist.js 2>/dev/null || true",
    "icons": "node scripts/copy-icons.js",
    "cap:sync": "npx cap sync ios",
    "cap:open": "npx cap open ios",
    "build:ios-prep": "npm install && npx cap add ios 2>/dev/null || true && npm run icons && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/splash-screen": "^6.0.3",
    "@capacitor/status-bar": "^6.0.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  },
  "capacitor": {
    "comment": "Voir capacitor.config.json — URL Beer distante (pas de copie du front)"
  }
}